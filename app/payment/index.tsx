/** @format */

import {
	Image,
	SafeAreaViewBase,
	Text,
	TouchableOpacity,
	View,
} from 'react-native';
import React from 'react';
import icons from '../../components/icons/Icons';
import { Platform } from 'react-native';
import { SafeAreaView } from 'react-native';

export default function RedeemPaymentModal() {
	return (
		<SafeAreaView className='flex-1 bg-[#FAFAFA] px-4 justify-center'>
			
		</SafeAreaView>
	);
}
